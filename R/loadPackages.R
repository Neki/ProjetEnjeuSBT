# Load required packages
# This script is to be called at the begginning of the app

# TODO : add error check
# TODO : check presence of correct .R scripts
# TODO : remove it, not necessary in a package code

library(RGtk2, quietly = TRUE)
library(cairoDevice, quietly = TRUE)
library(VennDiagram, quietly = TRUE)
